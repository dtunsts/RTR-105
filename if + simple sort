#include <stdio.h>

int main()
{
int a = 0;
int b = 0;
int c = 0;
char s;
printf("pirmais skaitlis:");
scanf("%d", &a);
printf("otrais skaitlis:");
scanf("%d", &b);
printf("trešais skaitlis:");
scanf("%d", &c);
printf("skatilu seciba augoša - a ; dilstoša - d \n");
scanf("%s", &s);

printf("vai simbols ir a %d\n",s=='a' );
if (s == 'a') { 
}    
if (a>b>c) {
printf (" %d %d %d/n ",a ,b ,c);
}
else if (a>c>b) {
    printf (" %d %d %d/n ",a ,c ,b);
    
}

}
